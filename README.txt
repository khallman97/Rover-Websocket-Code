Our websocket server code. Currently we are able to connect the code on 1 dervice as a client and a server.
Our next goal is to be able to have the client be on any computer but on the same netword and send
commands to the server. Also as a future goal we want to be able to have the server send information to the
client